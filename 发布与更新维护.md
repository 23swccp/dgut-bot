# 发布与更新维护

本文记录莞工小皮卡当前的 Windows 安装、Velopack 自动更新、GitHub Release 与 Gitee 镜像方案。修改发布逻辑前必须先读本文。

## 当前分工

| 位置 | 用途 |
| --- | --- |
| [GitHub](https://github.com/23swccp/dgut-bot) | 主仓库、GitHub Actions、正式 Release、应用内 Velopack 更新源 |
| [Gitee](https://gitee.com/swccq23/dgut-bot) | 国内源码镜像和首次安装器备用下载 |
| `.github/workflows/release.yml` | 推送 `v*` 标签后测试、构建、发布 GitHub Release，并把安装器上传到 Gitee |
| `.github/workflows/sync-gitee.yml` | 推送 `main` 或 `v*` 标签后，将 GitHub 分支和标签快进同步到 Gitee |
| `.github/workflows/gitee-release.yml` | 手动把某个已有 GitHub Release 的安装器补发到 Gitee |
| `scripts/publish_gitee_release.py` | 幂等创建/复用 Gitee Release，并替换同名安装器附件 |

应用内更新只从 GitHub 获取 `releases.win.json` 和 `.nupkg`。Gitee 不参与应用内更新，只用于解决部分用户无法顺利下载首次安装器的问题。

## 用户应该下载什么

普通用户只下载：

```text
DgutBot-win-Setup-Qing-Zhi-Xia-Zai-Zhe-Ge.exe
```

该文件是包含完整程序的安装器，不是联网引导器。从 GitHub 或 Gitee 下载完成后，可以断网安装。首次启动会尝试访问 GitHub 检查更新；检查失败不会影响当前版本运行。

GitHub Release 中以下文件不能删除，它们由 Velopack 自动更新使用：

- `DgutBot-<version>-full.nupkg`
- `DgutBot-<version>-delta.nupkg`（有历史版本时生成）
- `RELEASES`
- `releases.win.json`

Gitee Release 只主动上传用户安装器；页面可能另外显示 Gitee 自动生成的源码压缩包。

## 令牌与安全

GitHub 仓库已经配置 Actions Secret `GITEE_TOKEN`。它用于：

- 向 Gitee 推送 `main` 和版本标签；
- 创建 Gitee Release；
- 上传或替换同名安装器附件。

令牌值不能写入仓库、日志、命令行参数示例或聊天记录。正常情况下无需再次输入。令牌被撤销或失效时，由仓库维护者在项目目录运行：

```powershell
gh secret set GITEE_TOKEN
```

然后按提示粘贴新令牌。GitHub Secret 只能覆盖，不能读取回明文。

## 哪些修改需要新安装器

- 只改 `README.md`、开发文档或 Release 文字：不需要发布新版本。
- 修改 Python、前端、资源或依赖：必须发布新版本。
- 不要悄悄替换既有标签下的 `Setup.exe`。即使只改程序内文档，也应增加补丁版本，例如 `0.3.2` → `0.3.3`。

## 版本位置

发布时检查并同步：

1. `version.py`：`APP_VERSION`，运行时和发布工作流的权威版本。
2. `web/package.json` 与 `web/package-lock.json` 顶部项目版本。
3. `web/public/docs/badges/version.svg` 及其 GitHub README 镜像 `docs/badges/version.svg` 的可见文字和无障碍标签。
4. `.github/workflows/gitee-release.yml` 的手动补发默认标签（仅为操作便利）。

测试里的旧版本字符串可能是测试数据，不要机械替换。

## 正式发布步骤

假设新版本为 `0.3.3`：

1. 更新上述版本位置并确认文档内容。
2. 运行完整验证：

```powershell
python -m pytest -q
cd web
npm test -- --run
npm run build
cd ..
```

3. 提交并先推送 `main`：

```powershell
git add -- <本次文件>
git commit -m "release: prepare v0.3.3"
git push origin main
```

4. 确认远端不存在同名标签，再创建并推送带说明标签：

```powershell
git ls-remote --tags origin refs/tags/v0.3.3
git tag -a v0.3.3 -m "v0.3.3"
git push origin v0.3.3
```

5. 等待 `Release` 和 `Sync source to Gitee` 两个 GitHub Actions 工作流完成。`release.yml` 会执行前后端测试、PyInstaller 构建、打包后冒烟测试、Velopack 打包、GitHub 发布和 Gitee 安装器上传。

## 发布后验收

至少确认：

- GitHub Release 不是草稿或预发布，版本号正确。
- GitHub 中用户安装器已改成带“Qing-Zhi-Xia-Zai-Zhe-Ge”的名称。
- `.nupkg`、`RELEASES`、`releases.win.json` 仍存在。
- Gitee 同版本 Release 存在，安装器无需登录即可下载。
- GitHub 与 Gitee 的 `main` 和版本标签指向同一提交。
- Gitee 安装器的公开下载响应为 Windows 可执行文件，文件大小与 GitHub 一致。
- 从旧安装版启动时能发现新版本，下载后程序进程正确退出并由 Velopack 完成替换、重启。
- 在干净虚拟机中从 Gitee 下载完整安装器，关闭 VPN/加速器后仍能完成安装与启动。

## 故障处理

### Gitee Release 缺失

在 GitHub Actions 手动运行 `Publish existing release to Gitee`，输入已经存在的 GitHub 标签。它会下载 GitHub 的用户安装器，再调用 `scripts/publish_gitee_release.py` 补发。

### Gitee 上传很慢或超时

GitHub 托管运行器到 Gitee 可能很慢。上传脚本使用较长的写入超时，并且可以安全重跑：已有 Release 会复用，同名附件会先删除再上传。

### 源码镜像不同步

检查 `Sync source to Gitee` 工作流。同步只允许正常快进，不应强制覆盖 Gitee 的分叉历史。修复令牌或分支问题后可手动重新运行。

### 应用内更新失败

应用内更新源仍是 GitHub，Gitee 安装器镜像不能替代 Velopack 更新源。提示用户开启支持 GitHub 的网络加速器后重试；当前已安装版本仍可继续使用。

v0.3.5 为检查增加 20 秒等待上限，超时后在设置页显示错误。Velopack 1.2.0 不支持取消原生网络请求，超时后的结果会被丢弃；旧请求尚未结束时不会叠加新请求，持续无响应需重启程序后重试。此修复包含在 v0.3.5 安装器中，不能替换 v0.3.4 安装器。
