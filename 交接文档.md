# 维护交接

本文件只记录当前结构与维护边界；历史验收记录、已完成的重构计划和页面调研稿均已移除。使用说明在 `README.md`，发布与自动更新规则在 `发布与更新维护.md`。

## 目录地图

```text
src/dgutbot/
├─ app/       启动器、本机 HTTP 服务、命令适配、浏览器与更新集成
├─ agent/     JSON CLI、协议、运行时、任务与工具注册
├─ course/    课件状态机、测验、弹窗、PPT 与答题请求
└─ domain/    优学院登录、课程、签到、配置和日志
tests/        全部离线回归测试
tools/        诊断、模拟与手工调试脚本，不进入发行包
web/          React/Vite 前端
docs/         GitHub README 图片镜像（与 web/public/docs 保持一致）
scripts/      构建、冒烟测试和发布辅助脚本
version.py    唯一版本权威（发布规则要求保留在根目录）
```

## 模块职责

| 位置 | 职责 |
| --- | --- |
| `app/browser_launcher.py` | GUI/后台服务入口、端口选择、浏览器启动和关闭生命周期 |
| `app/campus_startup.py` | Windows 当前用户校园网登录页启动项；`--campus-login` 只打开固定入口，不启动主界面 |
| `app/web_server.py` | 仅回环地址的 HTTP API 与发行版前端托管 |
| `app/backend_commands.py` | 前端命令的兼容适配、事件流与应用服务组合 |
| `app/browser_*.py` | 浏览器路径、原生选择、标签生命周期与单实例支持 |
| `app/velopack_updater.py` | Velopack 更新状态适配，不自行替换程序文件 |
| `domain/yxy_backend.py` | 登录缓存、同源学校页面浏览器请求桥、课程、签到、配置与课件控制器组合 |
| `domain/course_scan.py` | 未完成课件目录适配、完成状态判断、稳定去重、并发扫描、取消与五分钟缓存 |
| `course/yxy_course.py` | CDP 课件状态机、视频/文档/PPT、导航与恢复 |
| `course/yxy_quiz.py` | 测验读取、GUI 固定策略与 Agent 执行器 |
| `course/course_dialogs.py`、`course_slides.py` | 可验证的弹窗分流和 PPT 页面读取/坐标计算 |
| `agent/` | 对外 JSON 协议、认证运行时、任务幂等与工具服务 |
| `tools/` | `quiz_simulator` 验证弹窗/PPT；`course_lab` 验证真实浏览器 + 服务 + CLI 测验链路；其余为维护诊断工具 |
| `web/src/signObservability.ts` | 签到事件去重、轮询噪音过滤和面向界面的事件文案转换 |

## 签到界面状态约定

- `get_sign_monitor_status` 返回当前课程、监测状态、检查间隔、轮次、最近检查时间和最近结果；前端每秒读取一次，用于更新底部状态栏和事件流末尾的持续监测节点。
- 普通的“第 N 轮正在检查”不加入可见事件流，相同课程的相同空结果在当前页面会话中只显示一次；签到成功、跳过、警告和错误仍作为独立事件保留。
- 签到页采用一条连续的流程线：关键事件 → 命令节点 → 缩进后的课程分支。底部状态栏不属于事件历史，不要把每秒变化的倒计时复制进事件列表。
- 程序内“关于”页直接渲染根目录 `README.md`，因此用户操作或界面语义变化应优先同步到 README，无需在 `AboutGuide.tsx` 中维护第二份教程正文。
- README 图片使用 `./docs/...`，使 GitHub 仓库页和程序内关于页都能解析；根目录 `docs/` 是 `web/public/docs/` 的 GitHub 展示镜像，修改图片或版本徽章时必须同步两处。
- 从浏览器读取到的 `AUTHORIZATION` Cookie 必须先通过课程请求验证，验证成功后才能写入 `auth.json` 或显示登录成功。登录重定向在浏览器 `fetch` 中可能只表现为 `TypeError`；它与 HTTP 401 都按凭证失效处理。同一个已拒绝 Cookie 在自动登录轮询中不重复请求，等待浏览器产生新值后再继续。
- 未完成课件扫描仍通过 `BrowserApiClient` 的 Chromium 同源请求桥。2026-09-10 实页采样确认目录链路为教材列表 `textbook/student/{ocId}/list`、学生班级信息 `classes/information/student/{ocId}` 和教材详情 `textbook/student/information`；详情中的教材 `courseId`、条目 `currentUnitID`、班级 `classId` 分别对应 UA 地址的 `courseId`、`chapterId`、`classId`。`progress=1.0` 表示完成，`hide=2` 且 `planState.studyState=3` 的计划课件尚未开放。字段适配集中在 `domain/course_scan.py` 和 `SignBackend._course_directory_payload`。扫描允许单课程失败、最多四路并发、取消及约五分钟内存缓存；自动打开校验选中标识后复用原课件控制器，手动打开再启动的路径始终保留。

## 校园网开机入口

- 设置中的“开机时打开校园网登录页”通过当前用户注册表 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 管理 `DgutBotCampusLogin`，不需要管理员权限。
- 启动项执行 `--campus-login`，立即交给 Windows 默认浏览器打开 `https://login.dgut.edu.cn/eportal/index.jsp`；不等待网络、不探测联网状态，也不保存用户提供的带 IP、MAC、NAS 或 `userIndex` 参数的动态链接。
- 冻结版启动命令指向安装目录中的 `dgut-bot.exe`；源码开发环境使用 `scripts/campus_login_startup.pyw`，避免弹出控制台。正常启动冻结版时会在开关启用的情况下刷新注册表中的 EXE 路径，以适应更新后的安装位置。

## 运行与验证

- Windows 用户入口：`启动浏览器版.bat`（执行 `python -m dgutbot.app.browser_launcher`）。
- Agent CLI：`python -m dgutbot.agent.agent_cli capabilities`；它只连接已启动的本机服务。
- 开发环境使用 `pyproject.toml` 将 `src` 加入测试导入路径；不要重新把模块散放到根目录。
- 常规验证：`python -m pytest -q`，再在 `web/` 中运行 `npm test -- --run` 和 `npm run build`。
- 修改启动或生命周期逻辑后额外运行 `python -m dgutbot.app.browser_launcher --check`。
- Agent/课件变更运行 `python tools/course_lab.py`；场景、报告及真实样本校准说明见 `课件测试环境.md`。
- 真实页面只读采样入口为 `tools/quiz_probe.py`，实现分为 `course_capture.py`、`capture_privacy.py`、`capture_dom.js`；流程及脱敏边界见 `课件采样说明.md`。修改后运行 `tests/test_course_capture.py` 与 Chrome/Edge 的 `tools/capture_smoke.py --browser 路径`；真实样本保存在被忽略的 `.course-captures/`，不可提交。
- 根目录 `yxy_capture_fixed.py` 是本机协议调查脚本，不进入正常应用链路。输入 `s` 开始抓包后，默认每 120 秒（2 分钟）刷新一次东莞理工/优学院页面；`AUTO_REFRESH_INTERVAL=0` 可关闭。其桌面抓包日志可能含登录凭据与真实课程数据，不得提交。

## 不可突破的边界

- 页面注入脚本只观察页面；所有点击、输入和提交必须经 Python 的 CDP Input，并验证页面结果。
- 未识别的题型、弹窗和播放器控件不盲点、不自动跳过，保留可诊断状态。
- 不提交账号、Token、浏览器资料、日志或真实课程数据。
- 修改发布、安装器、版本或 Gitee 同步前，完整阅读 `发布与更新维护.md`；不要替换已发布安装器。
