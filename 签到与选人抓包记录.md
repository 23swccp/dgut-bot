# 签到与选人抓包记录（2026-09-22）

本页记录官方优学院 Android 应用经 Reqable 抓到的接口行为，供后续维护使用。原始 HAR 含认证信息及真实师生数据，只保存在本机，不纳入仓库。本页省略账号、活动编号、签到码实际值和人员信息。

## 手机签到

- 上午的 HAR 包含一次 `POST https://application.dgut.edu.cn/classroomapi/newAttendance/signByStu`，请求体字段为 `attendanceID`、`classID`、`userID`、`location`、`address`、`enterWay`、`attendanceCode`。
- 本次 `attendanceCode` 是四位数字；`location` 和 `address` 均为空字符串；`enterWay=1`。这只证明本次签到的实际提交内容，不说明所有签到类型都允许空定位。
- 请求带 `AUTHORIZATION`、`Origin: https://lms.dgut.edu.cn`、`Referer: https://lms.dgut.edu.cn/`、`X-Requested-With: cn.ulearning.yxy`，并使用 Android WebView 的请求头。未观察到这次 POST 携带 Cookie。
- POST 的 HTTP 状态为 200，业务响应包含 `status=200`、`newStatus=1`。同一活动的 `GET /apps/newAttendance/getAttendanceForStu/{attendanceID}/{userID}` 在提交前返回 `status=0`，提交后返回 `status=1`。因此本次**手机**签到成功得到请求和后续状态的双重印证，不能据此宣称程序曾完成签到。
- 现有程序从活动数据中提取签到码；取不到时依用户要求仍提交空 `attendanceCode` 并记录服务端结果。这种空码请求尚未由本次 HAR 验证为可成功签到。二维码内容的生成规则及如何从教室二维码取得有效码仍未知。

## 选人

- 下午两份 HAR 指向同一个选人活动。活动列表将其标为 `relationType=10`；程序的签到轮询只处理 `relationType=1`，因此不会把选人活动当作签到提交。
- 学生端反复 `GET https://lms.dgut.edu.cn/apps/selectperson/getDetail`，查询参数为 `userId`、`id`、`roleId`；响应包含 `students` 列表。第一份记录 5 次查询，列表为 1 人；第二份约 10 分钟内有 289 次查询，列表从 10 人逐步增至 15 人。两份中当前账号均未出现在该列表。
- 未观察到学生端提交选人、修改候选资格或改变抽选结果的请求。关闭轮询或修改本地显示不会改变服务器的选人结果。
- 本次没有“本人被隐藏选中且尚未公布”的样本。能否在公布前通过学生端响应识别隐藏选人，仍需该场景前后的抓包验证。

## 后续验证边界

程序已按手机 POST 更新请求头，但还没有在新的签到活动中完成一次可归因于程序的真实提交。下一次验证应同时核对程序请求、业务响应及随后签到状态；仅凭 HTTP 200 或手机最终已签到，无法判断是哪一端完成的。
