# 课件测试环境

这个实验室无需真实账号和课件，用真实 Chrome/Edge、生产本机 HTTP 服务、CLI 子进程、课程状态机和 CDP 输入执行完整测验流程。所有题目和保存记录都是本地生成的合成数据。

它能发现已有页面契约下的回归、时序、输入、任务生命周期和重复提交问题。它不能证明真实平台故障概率低于某个百分比；真实站点的 DOM、播放器、登录、接口规则变化仍需少量真实样本校准。

## 一键运行

在项目根目录运行：

```powershell
python -m pip install -r requirements.txt pytest
python tools/course_lab.py
```

程序自动寻找 Chromium，创建独立浏览器资料目录、本地站点、随机端口和临时运行凭据。测试不复用你的登录浏览器、配置或真实课程；不需要启动日常使用的程序。结束后关闭自己创建的浏览器和服务，移除临时数据。

显示测试窗口、选择浏览器、复现单个种子：

```powershell
python tools/course_lab.py --show --case roundtrip
python tools/course_lab.py --browser "C:\Program Files\Google\Chrome\Application\chrome.exe" --repeat 3
python tools/course_lab.py --browser "C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe" --repeat 3
python tools/course_lab.py --case delayed --case stale_page --seed 20260904
```

每轮变更数字、判断结果和选项顺序。`--repeat` 是确定性变体回归，不是对真实世界错误率的统计抽样。

结果写入 `.course-lab/<时间与运行ID>/`（已忽略，不提交仓库）：

- `report.json`：浏览器版本、每个场景的种子、耗时和通过状态。
- `<场景>-<种子>.json`：CLI 请求/响应、任务事件、模拟站点收到的答案及网页状态。
- `<场景>-<种子>.png`：失败现场截图。

失败记录还包含浏览器滚动、指针和焦点事件时间线，用于区分题目解析错误、页面布局变化与点击未命中。

退出码 0 表示全部通过；任何断言失败返回非零。报告只包含本地合成数据，不包含临时鉴权凭据和浏览器资料。

## 测试经过的真实代码

`CLI 子进程 → 鉴权 HTTP API → AgentService → CourseController → 测验 JSON → 参考答题端 → CLI → QuizExecutor → CDP Input → 浏览器 fetch → 本地站点保存校验 → 页面完成标记 → 任务继续/结束`

参考答题端仅根据 CLI 返回的题干和选项解答简单计算、判断及双填空；服务器另行保留预期答案并校验实际提交。没有直接调用答题执行器、伪造 CLI 成功响应或通过脚本填写答案。只有站点 URL 匹配和调试端口在测试进程中替换，其他业务流程使用生产实现。这里覆盖服务/CLI/控制器的集成，不覆盖 GUI 启动器或安装器。

参考答题端替代大模型，是为了稳定验证程序通路；它不衡量 AI 对真实课程知识、图片、公式的理解和正确率。

## 场景与通过标准

| 场景 | 通过标准 |
| --- | --- |
| `roundtrip` | 连续两页取题、作答、各提交一次、自动翻页并完成课程 |
| `document` | 末页纯文档自动滚动到底后完成，不因末页判断变化而提前结束或卡住 |
| `timed` | 开始答题按钮通过真实输入触发一次，随后正常提交 |
| `long_narrow` | 620px 窗口、题目超出视口，滚动定位和双填空正确 |
| `delayed` | 保存响应延迟 1.5 秒，等待完成，不重复提交 |
| `slow_render` | 控制器启动后延迟挂载题目，仍发现题目并完成 |
| `rerender` | 取题后替换 DOM 节点，内容不变，重新定位后完成 |
| `invalid` | 缺答案和错误 revision 被拒绝，校验不改网页，修正后完成 |
| `stale_page` / `stale_structure` | 取题后页标识/题干变化，拒绝旧答案，无点击、输入和提交 |
| `unsupported` | 多选题明确标记不支持，整套拒绝，无猜测操作 |
| `expired` / `cancelled` | 超时/取消后答案不可再执行 |
| `occluded` | 输入目标被遮挡，不能确认已填写时不提交 |
| `prefilled` | 填空已有不同文本时失败，不覆盖原答案或提交 |
| `multi_blank` | 按真实多填空结构填写三个输入框，单次提交后输入框锁定并生成逐空结果状态 |
| `graded_wrong` | 答案被平台判错但保存成功，题目仍锁定并完成，不能把知识错误误报为提交失败 |
| `server_error` | HTTP 503 后不能确认完成，报告失败且不重试提交 |
| `response_lost` | 服务端保存后丢失回执，报告未验证完成，不重复保存 |
| `late_response` | 回执晚于验证窗口，保持可诊断失败，迟到完成不触发再提交 |

正常提交场景还验证立即重放幂等请求只执行一次；故障场景验证结束后重放不会补点提交。所有场景检查浏览器收到的点击和输入为可信事件。

这套实验室搭建时实际捕获并修复了两个问题：末页题目尚未加载时误报课程完成；Chromium 输入后的光标自动滚动与下一次定位竞争，导致填空/提交未命中。相应场景已保留，后续修改可持续回归。

## 配套回归与自动运行

```powershell
python -m pytest -q
python tools/quiz_simulator.py
```

原有模拟器负责固定答题策略、已识别弹窗、限时入口及同源/跨域 PPT；新实验室负责完整 Agent/CLI 流程。两套组合使用，不能把某套通过等同于全部模块通过。

`.github/workflows/course-lab.yml` 在相关 Pull Request 和手动触发时运行 Windows Chrome/Edge 矩阵、两轮 CLI 场景，并上传合成报告与失败截图。工作流需要推送到 GitHub 后才会运行，本地创建文件不代表已完成云端验证。

## 如何用少量真实课件提高可信度

现有 `tools/quiz_probe.py` 已扩展为只读时序采样器：支持同源/跨域嵌套框架、控件差异、接口字段结构和覆盖报告，使用步骤见 [课件采样说明](课件采样说明.md)。导出的静态结构仍需加工为合成 fixture，不会自动变成真实平台的完整重放。

按“结构类别”收集样本，而不是追求大量重复课件：普通/限时测验、不同填空控件、图片或公式、视频播放器、同源/跨域文档、各种阻塞弹窗分别选代表。保留脱敏后的 DOM 结构、控件属性和事件顺序，用合成题干替换内容后纳入 fixture；不保存 Cookie、Token、账户信息或原始课程正文。

遇到真实故障，先把最小触发条件做成本地回归场景，再修复和重复验证。没有样本的多选、图片/公式、第三方播放器和平台业务规则变化应标明“未覆盖”，不能拿现有通过数作保证。真实平台只需承担这些结构校准和少量冒烟验证，大多数开发回归都可以留在本地。
